import { Truck, MessageCircle, RefreshCw, ShieldCheck, LucideIcon } from 'lucide-react';

// Icônes de la section "Pourquoi choisir Ladies Dress".
// Les TITRES et TEXTES de chaque avantage restent dans messages/*.json
// (clé "home.whyUsItems", un tableau traduit en FR/AR/EN) — ce fichier ne
// gère que l'icône associée à chaque position, dans le même ordre.
export const serviceIcons: LucideIcon[] = [Truck, MessageCircle, RefreshCw, ShieldCheck];
